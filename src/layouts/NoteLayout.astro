---
import Layout from '@layouts/Layout.astro';

interface Props {
    title: string;
    description: string;
}
---

<Layout {...Astro.props}>
    <h1>{Astro.props.title}</h1>
    <slot />
</Layout>
