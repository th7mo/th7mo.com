---
import type { CollectionEntry } from "astro:content";

import Layout from "@layouts/Layout.astro";
import AlphabetNavigation from "@components/AlphabetNavigation.astro";
import NoteCardCollection from "@components/NoteCardCollection.astro";

interface Props {
    title: string;
    notes: CollectionEntry<"notes">[];
}
const { title, notes } = Astro.props;
---

<Layout title={title} description="A collection of my scattered thoughts.">
    <h1>{title}</h1>
    <p>A collection of my <code>{notes.length}</code> scattered thoughts.</p>
    <AlphabetNavigation notes={notes} />
    <NoteCardCollection notes={notes} />
</Layout>

<style>
    code {
        font-weight: 700;
    }
</style>
