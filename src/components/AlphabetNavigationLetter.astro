---
const { letter } = Astro.props;
---
<li>
    <a class="navigation-card" href={`${Astro.url.pathname}#${letter}`}>{letter.toUpperCase()}</a>
</li>

<style>
    a {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 3rem;
        width: 3rem;
    }
</style>
